{
  "name": "pyramid",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "bin": {
    "pyramid": "lib/bin/index.js"
  },
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc -p .",
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/bin/index.ts",
    "start:dev": "nodemon --config nodemon.json",
    "test": "nodemon --config nodemon.test.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "image-size": "^0.8.3",
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^13.11.1",
    "@types/mkdirp": "^1.0.0",
    "nodemon": "^2.0.3",
    "ts-node": "^8.8.2",
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  }
}
